<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>pom</packaging>
	<artifactId>ss7-internal-components-checkout</artifactId>
	<groupId>org.mobicents</groupId>
	<version>1.0</version>

	<name>Mobicents :: SS7 :: ${pom.artifactId}</name>

	<properties>

	    <checkout.basedir>${checkout_base_dir}</checkout.basedir>
		<checkout.username>mobicents-read-only</checkout.username>
		<checkout.password></checkout.password>

		<protocol.connectionURL>scm:svn:http://mobicents.googlecode.com/svn/tags/protocols/ss7</protocol.connectionURL>
	</properties>

	<profiles>
		<profile>
			<id>checkout-protocol</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-scm-plugin</artifactId>
						<inherited>false</inherited>
						<executions>
							<execution>
								<id>checkout-protocol</id>
								<goals>
									<goal>checkout</goal>
								</goals>
								<phase>validate</phase>
								<configuration>
									<scmVersion>HEAD</scmVersion>
									<scmVersionType>revision</scmVersionType>
									<skipCheckoutIfExists>false</skipCheckoutIfExists>
									<checkoutDirectory>${checkout.basedir}/${protocol_name}</checkoutDirectory>
									<connectionUrl>${protocol.connectionURL}/${protocol_name}/${protocol_id}-${protocol_version}</connectionUrl>
									<username>${checkout.username}</username>
									<password>${checkout.password}</password>
								</configuration>
							</execution>
							<!-- 
							<execution>
								<id>ss7-core-docs-checkout</id>
								<goals>
									<goal>checkout</goal>
								</goals>
								<phase>validate</phase>
								<configuration>
									<scmVersion>HEAD</scmVersion>
									<scmVersionType>revision</scmVersionType>
									<skipCheckoutIfExists>false</skipCheckoutIfExists>
									<checkoutDirectory>${checkout.basedir}/core/docs</checkoutDirectory>
									<connectionUrl>${ss7.core.docs.connectionURL}-${docs_version}</connectionUrl>
									<username>${checkout.username}</username>
									<password>${checkout.password}</password>
								</configuration>
							</execution>
							 -->
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
	</profiles>
	
</project>
