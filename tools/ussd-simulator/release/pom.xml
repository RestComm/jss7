<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>pom</packaging>

	<parent>
		<groupId>org.mobicents.protocols.ss7.tools.simulator</groupId>
		<artifactId>parent</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>sources-checkout</artifactId>
	<groupId>org.mobicents.protocols.ss7.tools.simulator</groupId>
	<version>1.0.0-SNAPSHOT</version>

	<properties>

		<checkout.basedir>${basedir}/checkout</checkout.basedir>
		<checkout.username>ze carlos</checkout.username>
		<checkout.password />
		<tag.version>${pom.version}</tag.version>

		<!-- TAGS 
 		 
		<simulator.connectionURL>scm:svn:http://mobicents.googlecode.com/svn/tags/applications/ss7-simulator/${tag.version}</simulator.connectionURL>
		-->
		<!-- TRUNK -->
		<simulator.connectionURL>scm:svn:http://mobicents.googlecode.com/svn/trunk/applications/ss7-simulator</simulator.connectionURL>
	</properties>

	<profiles>
		<profile>
			<id>checkout</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-scm-plugin</artifactId>
						<inherited>false</inherited>
						<executions>
							<execution>
								<id>core-checkout</id>
								<goals>
									<goal>checkout</goal>
								</goals>
								<phase>validate</phase>
								<configuration>
									<scmVersion>HEAD</scmVersion>
									<scmVersionType>revision</scmVersionType>
									<skipCheckoutIfExists>true</skipCheckoutIfExists>
									<checkoutDirectory>
								${checkout.basedir}
							</checkoutDirectory>
									<connectionUrl>
								${simulator.connectionURL}
							</connectionUrl>
									<username>${checkout.username}</username>
									<password>${checkout.password}</password>
								</configuration>
							</execution>


						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

	</profiles>


	<repositories>
		<repository>
			<id>jboss-public-repository-group</id>
			<name>JBoss Public Maven Repository Group</name>
			<url>https://repository.jboss.org/nexus/content/groups/public</url>
			<layout>default</layout>
			<releases>
				<enabled>true</enabled>
				<updatePolicy>never</updatePolicy>
			</releases>
			<snapshots>
				<enabled>true</enabled>
				<updatePolicy>never</updatePolicy>
			</snapshots>
		</repository>
	</repositories>

</project>
